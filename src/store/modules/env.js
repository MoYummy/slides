const state = {
  packageJson: JSON.parse(unescape(process.env.PACKAGE_JSON))
}

const getters = {

}

const actions = {

}

const mutations = {

}

export default {
  state,
  getters,
  actions,
  mutations
}